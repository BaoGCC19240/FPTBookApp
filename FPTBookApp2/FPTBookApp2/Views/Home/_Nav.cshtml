@model IEnumerable<FPTBookApp2.Models.category>
<li>
    <div class="dropdowna">
        <button class="dropbtna">
            Product
            <i class="fa fa-caret-down"></i>
        </button>
        <div class="dropdown-contenta">
            @foreach (var catitem in Model)
            {
                <a>@Html.DisplayFor(m => catitem.CatName)</a>
            }

        </div>

    </div>
</li>
